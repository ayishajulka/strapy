'use strict';

/**
 * top-destination-footer controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::top-destination-footer.top-destination-footer');
